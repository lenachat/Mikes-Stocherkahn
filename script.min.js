AOS.init(), document.addEventListener("DOMContentLoaded", () => { let e = document.querySelector(".bg-video"); e && (e.muted = !0, e.play(), e.loop = !0) }); const modal = document.getElementById("modal"), closeModal = document.getElementById("closeModal"), banner = document.getElementById("newsBanner"); window.addEventListener("DOMContentLoaded", () => { modal.style.display = "block" }), closeModal.onclick = () => { modal.style.display = "none" }, banner.onclick = () => { modal.style.display = "block" }, window.onclick = function (e) { e.target == modal && (modal.style.display = "none") }; const closeButtons = document.querySelectorAll(".closeModal"); function validatePrivacyCheckbox() { let e = document.getElementById("privacy"); return e.classList.remove("checkbox-error"), !!e.checked || (e.classList.add("checkbox-error"), !1) } closeButtons.forEach(e => { e.addEventListener("click", () => { modal.style.display = "none" }) }), document.addEventListener("DOMContentLoaded", e => { gsap.registerPlugin(ScrollTrigger, TextPlugin); let t = gsap.utils.toArray(".desktopContentSection:not(:first-child)"), o = gsap.utils.toArray(".desktopPhoto:not(:first-child)"); gsap.set(o, { yPercent: 101 }); let l = gsap.utils.toArray(".desktopPhoto"); gsap.matchMedia().add("(min-width: 1051px)", () => { console.log("desktop"), gsap.to(".right", { scrollTrigger: { trigger: ".gallery", start: "top top", end: "bottom bottom", pin: ".right", pinSpacing: !1, markers: !1 } }), t.forEach((e, t) => { let n = e.querySelector("h3"), a = gsap.timeline().to(o[t], { yPercent: 0 }).set(l[t], { autoAlpha: 0 }); ScrollTrigger.create({ trigger: n, start: "top 70%", end: "top 40%", animation: a, scrub: 1, markers: !1, pinSpacing: !1 }) }) }) }); const faqItems = document.querySelectorAll(".faq-item"); faqItems.forEach(e => { let t = e.querySelector(".faq-question"); t.addEventListener("click", () => { e.classList.toggle("active") }) });